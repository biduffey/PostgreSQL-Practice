# ssh key with fish

ssh-keygen -t rsa -b 4096 -C "biduffey@gmail.com"
eval (ssh-agent -c)
ssh-add ~/.ssh/id_rsa

use cat to view key:
cat ~/.ssh/id_rsa.pub

~ [i] ⋊> ssh -T git@github.com
Hi biduffey! You've successfully authenticated, but GitHub does not provide shell access.
